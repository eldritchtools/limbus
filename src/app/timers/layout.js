export const metadata = {
    title: "Timers | Limbus Company Tools",
    description: "Relevant timers for game content"
};

export default function TimersLayout({ children }) {
    return <>{children}</>
}