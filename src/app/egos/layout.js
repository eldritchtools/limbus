export const metadata = {
    title: "E.G.Os | Limbus Company Tools",
    description: "Browse information on E.G.Os"
};

export default function EgosLayout({ children }) {
  return <>{children}</>;
}
