export const metadata = {
    title: "Contact/Feedback | Limbus Company Tools",
    description: "Contact and Feedback page"
};

export default function FeedbackLayout({ children }) {
    return <>{children}</>
}