export const metadata = {
    title: "Profile | Limbus Company Tools",
    description: "View a user's profile and content"
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}