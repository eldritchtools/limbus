export const metadata = {
    title: "About",
    description: "Learn about Limbus Company Tools, its purpose, and how it’s built."
};

export default function AboutLayout({ children }) {
    return <>{children}</>
}