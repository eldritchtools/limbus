export const metadata = {
  title: "Edit Team Build | Limbus Company Tools",
  description: "Edit a team build",
  robots: {
    index: false,
    follow: false
  }
};

export default function EditBuildLayout({ children }) {
  return children;
}
