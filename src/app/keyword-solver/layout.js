export const metadata = {
    title: "Keyword Solver | Limbus Company Tools",
    description: "Find a team meets keyword requirements and fulfills the requested constraints."
};

export default function KeywordSolverLayout({ children }) {
    return <>{children}</>
}