export const metadata = {
  title: "New MD Plan | Limbus Company Tools",
  description: "Create a new md plan",
  robots: {
    index: false,
    follow: false
  }
};

export default function NewMdPlanLayout({ children }) {
  return children;
}
