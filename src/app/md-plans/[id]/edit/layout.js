export const metadata = {
  title: "Edit MD Plan",
  description: "Edit a md plan",
  robots: {
    index: false,
    follow: false
  }
};

export default function EditMdPlanLayout({ children }) {
  return children;
}
