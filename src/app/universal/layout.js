export const metadata = {
    title: "Universal Gifts & Gift Combos | Limbus Company Tools",
    description: "Universal Gifts & Gift Combos"
};

export default function UniversalLayout({ children }) {
    return <>{children}</>
}