export const metadata = {
    title: "Update History | Limbus Company Tools",
    description: "View update history"
};

export default function UpdateHistoryLayout({ children }) {
  return children;
}
